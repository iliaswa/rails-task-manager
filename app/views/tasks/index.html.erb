<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> <%= link_to "( delete", task_path(task) ,
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %> | <%= link_to "edit )", edit_path(task) %> </li>
  <% end %>
</ul>
